name: help_ukraine_widget_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
#  advanced_storybook: ^1.0.2
  flutterbook: ^0.1.6
  ionicons: ^0.2.1

  help_ukraine_widget:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  solid_lints: ^0.0.12

flutter:
  uses-material-design: true
